{
  "name": "lumi-app-finder-mcp",
  "version": "1.3.0",
  "private": true,
  "type": "module",
  "description": "First-party MCP app finder for 46 Lumi Studio iOS apps across 50 Apple locales",
  "files": [
    "server/index.mjs",
    "server/catalog-contract.mjs",
    "server/catalog.json",
    "ui/app-finder.html",
    "MCP_APP_NOTICES.txt",
    "THIRD_PARTY_NOTICES.txt",
    "PRIVACY.md"
  ],
  "scripts": {
    "build:ui": "node scripts/build-ui.mjs",
    "build:skill": "node scripts/build-skill-catalog.mjs",
    "sync:catalog": "node scripts/sync-catalog.mjs && node scripts/build-skill-catalog.mjs",
    "catalog:gate": "node scripts/sync-catalog.mjs --check",
    "test": "node scripts/build-skill-catalog.mjs --check && node scripts/build-ui.mjs --check && node --test",
    "smoke": "node scripts/mcp-smoke.mjs",
    "benchmark": "node scripts/benchmark.mjs",
    "validate": "mcpb validate manifest.json",
    "validate:registry": "node scripts/validate-registry-schema.mjs",
    "pack:mcpb": "node scripts/build-ui.mjs --check && mcpb validate manifest.json && python3 scripts/pack_bundle.py"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@anthropic-ai/mcpb": "2.1.2",
    "@modelcontextprotocol/ext-apps": "1.7.4",
    "ajv": "8.20.0",
    "ajv-formats": "3.0.1",
    "esbuild": "0.28.1",
    "stopword": "3.1.5"
  },
  "bin": {
    "lumi-app-finder": "server/index.mjs"
  }
}
